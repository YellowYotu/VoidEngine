# Void Engine

Void Engine is a personal C++ engine/editor project.

## Releases

Release installers will be published manually per version, for example:

- `VoidEngine-Setup-0.0.1.exe`
- `VoidEngine-Setup-0.0.2.exe`

A future website can list these versions and link directly to the corresponding GitHub Release assets.
